import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme)=>({
    usnChip : {
        marginRight : '20px',
    },
    navbar : {
        backgroundColor : '#6A353B',
    }
}));

export default useStyles;