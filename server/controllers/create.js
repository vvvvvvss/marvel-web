
export const createPR = (req , res) => {
    try {
        console.log(req.body);
    } catch (error) {
        console.log(error);
    }
}

export const createBlog = (req , res) => {
    try {
        console.log(req.body);
    } catch (error) {
        console.log(error);
    }
}